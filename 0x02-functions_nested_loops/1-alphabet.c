#include "main.h"
/**
 * main - assigns a random number to int n everytime
 * it executes, and prints it
 * Return: 0;
 */
void print_alphabet(void)
{
	char l = 'a';

	while (l <= 'z')
	{
		_putchar(l);
		l++;
	}
	_putchar('\n');
}
